---
name: example-skill
description: OpenClaw-specific skill overlay for validation
---

# OpenClaw Example Skill

This harness overlay replaces the shared `SKILL.md` for OpenClaw while shared supporting files such as `checklist.md` still come from `skills/example-skill/`.
