{
  "name": "agentpkg",
  "version": "0.1.0",
  "description": "Unified plugin distribution system for AI coding harnesses",
  "type": "module",
  "private": true,
  "packageManager": "bun@latest",
  "scripts": {
    "dev": "bun run ./src/cli.ts",
    "build": "bun scripts/build.ts",
    "install:local": "bun scripts/install.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "commander": "^13.1.0",
    "gray-matter": "^4.0.3"
  },
  "devDependencies": {
    "@types/bun": "^1.2.14",
    "typescript": "^5.8.3"
  }
}
